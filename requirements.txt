# Core
anthropic>=0.40.0          # Claude tailoring + fit scoring
playwright>=1.48.0         # headless render of the resume -> PDF
openpyxl>=3.1.0            # Excel application tracker
fastapi>=0.115.0           # local capture + review web server
uvicorn[standard]>=0.32.0  # ASGI server for FastAPI
pydantic>=2.9.0            # config + job models
PyYAML>=6.0               # config file
python-dotenv>=1.0.0       # load ANTHROPIC_API_KEY from .env
